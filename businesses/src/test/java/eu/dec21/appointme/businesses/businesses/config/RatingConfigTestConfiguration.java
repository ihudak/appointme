package eu.dec21.appointme.businesses.businesses.config;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Minimal Spring Boot configuration for testing RatingConfig.
 * This allows @SpringBootTest to start a minimal application context.
 */
@SpringBootApplication
public class RatingConfigTestConfiguration {
    // Minimal configuration - no beans needed
}
