application:
  file:
    upload-dir: ${BUSINESSES_FILES_DIR:./uploads/businesses/}
    max-file-size: 10MB


spring:
  banner:
    location: classpath:banner-dev.txt

server:
  port: ${SRV_PORT:8082}

categories:
  service:
    url: ${CATEGORIES_SERVICE_URL:http://localhost:8083}

feign:
  client:
    config:
      categories:
        connectTimeout: 5000
        readTimeout: 10000
        loggerLevel: basic